Archivo:
app/Views/inventario/assets/index.php

Dentro de cada fila busca:

<div class="table-actions">

Antes del botón Editar agrega:

<?php if (
    (bool) $asset['activo']
    && $asset['codigoEstado'] === 'EN_INVENTARIO'
): ?>
    <a
        class="button button--small"
        href="<?= e(
            base_url(
                'asignaciones/crear?activo='
                . $asset['idActivo']
            )
        ) ?>"
    >
        Asignar
    </a>
<?php endif; ?>

El sistema volverá a validar en el servicio que la copia esté activa,
disponible y sin otra asignación activa.
